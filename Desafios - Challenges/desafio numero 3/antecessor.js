function resultado(){
    var indice = Number(prompt("Escolha um número?"));
    var indicemenor = indice - 1;
    var indicemaior = indice + 1
    window.alert(`Antes do ${indice}, agora temos o número ${indicemenor}. \nDepois de ${indice}, temos o número ${indicemaior} `)
}